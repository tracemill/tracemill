type: job
name: AWS CloudTrail AWS ElastiCache Security Group Created - Sigma
detection:
  source: ngsiem_template
  name: "AWS - CloudTrail - AWS ElastiCache Security Group Created - Sigma"
  id: 4ae68615-866f-4304-b24b-ba048dfa5ca7
description: |
  An actor attempts to create an ElastiCache cache security group with
  CreateCacheSecurityGroup, a change to which EC2 security groups may reach
  cache clusters; the attempt is refused.

mitre:
  tactics: [defense-evasion]
  techniques: [T1562.007]

state:
  account_id: "111111111111"
  aws_region: us-east-1
  source_ip:  gen.ipv4()
  actor_name: gen.username()
  actor:      gen.aws_identity(type=IAMUser, userName=ref.actor_name, accountId=ref.account_id)
  user_agent: "aws-sdk-go-v2/1.36.5 ua/2.1 os/linux lang/go#1.24.4 md/GOOS#linux md/GOARCH#amd64 api/elasticache#1.46.0 tracemill/1.0"

workloads:
  - scenario: scenarios/aws/elasticache/create-cache-security-group-refused
    bindings:
      account_id: ref.account_id
      aws_region: ref.aws_region
      source_ip:  ref.source_ip
      actor:      ref.actor
      user_agent: ref.user_agent
    expectation:
      summary: "CreateCacheSecurityGroup -- attempt recorded with InvalidParameterValueException"
      expected: alert
