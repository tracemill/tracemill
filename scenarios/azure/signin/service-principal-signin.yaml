type: scenario
description: |
  A single successful Microsoft Entra ID (Azure AD) service principal
  sign-in: a SignInLogs record in the ServicePrincipalSignInLogs
  category, reporting a non-interactive authentication by an
  application's service principal (identified by servicePrincipalId /
  servicePrincipalName / appId, not a user UPN) rather than a human
  user. Service principals authenticate with a credential -- a secret
  or certificate -- an attacker can mint or steal without triggering
  interactive sign-in or MFA controls, so monitoring their
  authentication pattern (source IP, timing, accessed resource) is a
  documented control for spotting compromised app credentials.

  All identity fields (tenant, service principal id/name/app id,
  source IP) are bound by the job so the sign-in is internally
  consistent; the per-event sign-in record id is freshly generated so
  each event correlates individually.
mitre:
  tactics: [defense-evasion, initial-access, persistence, privilege-escalation]
  techniques: [T1078.004]
state:
  tenant_id:                    "11111111-1111-1111-1111-111111111111"
  src_ip:                       gen.ipv4()
  sp_id:                        gen.uuid()
  sp_name:                      "Research"
  app_id:                       gen.uuid()
  sp_credential_key_id:         gen.uuid()
  resource_display_name:        "Office 365 Management APIs"
  resource_app_id:              "c5393580-f805-4401-95e8-94b7a6ef2fc2"
  resource_service_principal_id: gen.uuid()
  city:                         "Boardman"
  region_state:                 "Ohio"
  country:                      "US"
steps:
  - emit:
      event_type: azure.monitor.aad@v1
      fields:
        resourceId: "/tenants/${ref.tenant_id}/providers/Microsoft.aadiam"
        operationName: "Sign-in activity"
        operationVersion: "1.0"
        category: ServicePrincipalSignInLogs
        tenantId: ref.tenant_id
        resultType: "0"
        resultSignature: "None"
        durationMs: 0
        callerIpAddress: ref.src_ip
        correlationId: gen.uuid()
        Level: 4
        location: ref.country
        properties:
          id: gen.uuid()
          createdDateTime: gen.timestamp()
          appId: ref.app_id
          ipAddress: ref.src_ip
          status:
            errorCode: 0
          location:
            city: ref.city
            state: ref.region_state
            countryOrRegion: ref.country
            geoCoordinates:
              latitude: 12.83
              longitude: -94.69
          correlationId: gen.uuid()
          conditionalAccessStatus: "notApplied"
          appliedConditionalAccessPolicies: []
          isInteractive: false
          tokenIssuerType: "AzureAD"
          authenticationProcessingDetails:
            - key: "Azure AD App Authentication Library"
              value: ""
          clientCredentialType: "none"
          processingTimeInMilliseconds: 0
          riskDetail: "none"
          riskLevelAggregated: "low"
          riskLevelDuringSignIn: "low"
          riskState: "none"
          resourceDisplayName: ref.resource_display_name
          resourceId: ref.resource_app_id
          servicePrincipalName: ref.sp_name
          servicePrincipalId: ref.sp_id
          flaggedForReview: false
          isTenantRestricted: false
          crossTenantAccessType: "none"
          servicePrincipalCredentialKeyId: ref.sp_credential_key_id
          incomingTokenType: "none"
          authenticationProtocol: "none"
          appServicePrincipalId: null
          resourceServicePrincipalId: ref.resource_service_principal_id
          uniqueTokenIdentifier: gen.regex(pattern=[A-Za-z0-9_-]{22})
          signInTokenProtectionStatus: "none"
          originalTransferMethod: "none"
