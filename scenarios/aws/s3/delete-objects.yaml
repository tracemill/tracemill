type: scenario
description: |
  Impact: an actor deletes a batch of objects from an S3 bucket with the
  multi-object DeleteObjects API. One call removes up to a thousand keys, so
  a handful of these events is the whole of a bulk deletion -- the volume
  lives inside the request, not in the CloudTrail event count.
  See https://docs.aws.amazon.com/AmazonS3/latest/API/API_DeleteObjects.html
mitre:
  tactics: [impact]
  techniques: [T1485]

state:
  aws_region:   us-west-2
  account_id:   "111111111111"
  user_name:    data-operator
  actor:        gen.aws_identity(type=IAMUser, userName=ref.user_name, accountId=ref.account_id)
  source_ip:    gen.ipv4()
  user_agent:   "Boto3/1.34.51 Python/3.11.8 Linux/5.15.0-101-generic Botocore/1.34.51 tracemill/1.0"
  bucket_name:  prod-data-backups
  bucket_arn:   "arn:aws:s3:::${ref.bucket_name}"
  bucket_host:  "${ref.bucket_name}.s3.${ref.aws_region}.amazonaws.com"
  tls_version:  "TLSv1.2"
  tls_cipher:   ECDHE-RSA-AES128-GCM-SHA256

steps:
  - emit:
      event_type: aws.cloudtrail@v1
      fields:
        eventVersion:       "1.11"
        eventTime:          gen.timestamp()
        eventID:            gen.uuid()
        eventSource:        s3.amazonaws.com
        eventName:          DeleteObjects
        eventType:          AwsApiCall
        eventCategory:      Data
        awsRegion:          ref.aws_region
        sourceIPAddress:    ref.source_ip
        userAgent:          ref.user_agent
        requestID:          gen.hex(len=16, case=upper)
        requestParameters:
          bucketName: ref.bucket_name
          Host:       ref.bucket_host
          delete:     ""
        responseElements:   null
        additionalEventData:
          SignatureVersion:     SigV4
          CipherSuite:          ref.tls_cipher
          bytesTransferredIn:   36864
          AuthenticationMethod: AuthHeader
          "x-amz-id-2":         gen.hex(len=76, case=lower)
          bytesTransferredOut:  27648
        readOnly:           false
        managementEvent:    false
        recipientAccountId: ref.account_id
        userIdentity:       ref.actor
        resources:
          - accountId: ref.account_id
            type:      "AWS::S3::Bucket"
            ARN:       ref.bucket_arn
        tlsDetails:
          tlsVersion:               ref.tls_version
          cipherSuite:              ref.tls_cipher
          clientProvidedHostHeader: ref.bucket_host
