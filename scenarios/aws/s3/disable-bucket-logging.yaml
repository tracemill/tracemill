type: scenario
description: |
  Defense evasion: an actor turns off server access logging on an S3 bucket
  by putting an empty logging status. Requests to the bucket stop being
  written to its access log from that point.
mitre:
  tactics: [defense-evasion]
  techniques: ["T1562.008"]

state:
  aws_region:  us-east-1
  account_id:  "000000000000"
  actor_name:  gen.username()
  actor:       gen.aws_identity(type=IAMUser, userName=ref.actor_name, accountId=ref.account_id)
  source_ip:   gen.ipv4()
  user_agent:  "[aws-sdk-go-v2/1.36.5 ua/2.1 os/linux lang/go#1.24.4 md/GOOS#linux md/GOARCH#amd64 api/s3#1.79.0 tracemill/1.0]"
  tls_version: "TLSv1.3"
  tls_cipher:  TLS_AES_128_GCM_SHA256
  request_id:  gen.regex(pattern=[A-Z0-9]{16})
  amz_id_2:    gen.regex(pattern=[A-Za-z0-9+/]{96})
  bucket_name: gen.regex(pattern=[a-z]{6,10}-(logs|data|assets)-[0-9]{4})
  bucket_host: "${ref.bucket_name}.s3.${ref.aws_region}.amazonaws.com"

steps:
  - emit:
      event_type: aws.cloudtrail@v1
      fields:
        eventVersion:       "1.11"
        eventTime:          gen.timestamp()
        eventID:            gen.uuid()
        eventSource:        s3.amazonaws.com
        eventName:          PutBucketLogging
        eventType:          AwsApiCall
        eventCategory:      Management
        awsRegion:          ref.aws_region
        sourceIPAddress:    ref.source_ip
        userAgent:          ref.user_agent
        requestID:          ref.request_id
        requestParameters:
          BucketLoggingStatus:
            xmlns: "http://s3.amazonaws.com/doc/2006-03-01/"
          Host:       ref.bucket_host
          bucketName: ref.bucket_name
          logging:    ""
        responseElements:   null
        additionalEventData:
          AuthenticationMethod: AuthHeader
          CipherSuite:          ref.tls_cipher
          SignatureVersion:     SigV4
          bytesTransferredIn:   71
          bytesTransferredOut:  0
          startTime:            gen.timestamp(format=millis)
          endTime:              gen.timestamp(format=millis, offset=86ms)
          httpStatusCode:       200
          x-amz-id-2:           ref.amz_id_2
        readOnly:           false
        managementEvent:    true
        recipientAccountId: ref.account_id
        userIdentity:       ref.actor
        resources:
          - ARN:       "arn:aws:s3:::${ref.bucket_name}"
            accountId: ref.account_id
            type:      "AWS::S3::Bucket"
        tlsDetails:
          tlsVersion:               ref.tls_version
          cipherSuite:              ref.tls_cipher
          clientProvidedHostHeader: ref.bucket_host
