type: scenario
description: |
  Persistence and execution: an actor replaces the user data of a stopped
  EC2 instance with ModifyInstanceAttribute. User data runs as root when
  the instance next boots, so rewriting it plants code that executes on
  the following start. CloudTrail records the attribute name and redacts
  the value.
mitre:
  tactics: [persistence, execution]
  techniques: ["T1059.009", "T1098"]

state:
  aws_region:  us-east-1
  account_id:  "000000000000"
  actor_name:  gen.username()
  actor:       gen.aws_identity(type=IAMUser, userName=ref.actor_name, accountId=ref.account_id)
  source_ip:   gen.ipv4()
  user_agent:  "aws-sdk-go-v2/1.36.5 ua/2.1 os/linux lang/go#1.24.4 md/GOOS#linux md/GOARCH#amd64 api/ec2#1.231.0 tracemill/1.0"
  tls_version: "TLSv1.3"
  tls_cipher:  TLS_AES_128_GCM_SHA256
  request_id:  gen.uuid()
  instance_hex: gen.hex(len=17, case=lower)
  instance_id:  "i-${ref.instance_hex}"

steps:
  - emit:
      event_type: aws.cloudtrail@v1
      fields:
        eventVersion:       "1.11"
        eventTime:          gen.timestamp()
        eventID:            gen.uuid()
        eventSource:        ec2.amazonaws.com
        eventName:          ModifyInstanceAttribute
        eventType:          AwsApiCall
        eventCategory:      Management
        awsRegion:          ref.aws_region
        sourceIPAddress:    ref.source_ip
        userAgent:          ref.user_agent
        requestID:          ref.request_id
        requestParameters:
          attribute:  userData
          instanceId: ref.instance_id
          value:      "<sensitiveDataRemoved>"
        responseElements:
          _return:   true
          requestId: ref.request_id
        readOnly:           false
        managementEvent:    true
        recipientAccountId: ref.account_id
        userIdentity:       ref.actor
        tlsDetails:
          tlsVersion:               ref.tls_version
          cipherSuite:              ref.tls_cipher
          clientProvidedHostHeader: "ec2.${ref.aws_region}.amazonaws.com"
