type: scenario
description: |
  Defense evasion: an actor turns off EBS encryption by default for the
  account in one region. New volumes and snapshot copies created there are
  no longer encrypted unless the caller asks for it, so data written to
  them can be read from an unencrypted copy.
mitre:
  tactics: [defense-evasion, impact]
  techniques: ["T1562", "T1486"]

state:
  aws_region:  us-east-1
  account_id:  "000000000000"
  actor_name:  gen.username()
  actor:       gen.aws_identity(type=IAMUser, userName=ref.actor_name, accountId=ref.account_id)
  source_ip:   gen.ipv4()
  user_agent:  "aws-sdk-go-v2/1.36.5 ua/2.1 os/linux lang/go#1.24.4 md/GOOS#linux md/GOARCH#amd64 api/ec2#1.231.0 tracemill/1.0"
  tls_version: "TLSv1.3"
  tls_cipher:  TLS_AES_128_GCM_SHA256
  request_id:  gen.uuid()

steps:
  - emit:
      event_type: aws.cloudtrail@v1
      fields:
        eventVersion:       "1.11"
        eventTime:          gen.timestamp()
        eventID:            gen.uuid()
        eventSource:        ec2.amazonaws.com
        eventName:          DisableEbsEncryptionByDefault
        eventType:          AwsApiCall
        eventCategory:      Management
        awsRegion:          ref.aws_region
        sourceIPAddress:    ref.source_ip
        userAgent:          ref.user_agent
        requestID:          ref.request_id
        requestParameters:
          DisableEbsEncryptionByDefaultRequest: ""
        responseElements:
          DisableEbsEncryptionByDefaultResponse:
            ebsEncryptionByDefault: false
            requestId: ref.request_id
            xmlns: "http://ec2.amazonaws.com/doc/2016-11-15/"
        readOnly:           false
        managementEvent:    true
        recipientAccountId: ref.account_id
        userIdentity:       ref.actor
        tlsDetails:
          tlsVersion:               ref.tls_version
          cipherSuite:              ref.tls_cipher
          clientProvidedHostHeader: "ec2.${ref.aws_region}.amazonaws.com"
